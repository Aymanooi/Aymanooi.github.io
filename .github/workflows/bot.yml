name: OKX Trading Bot — Snowball v22

on:
  # ⛔ متوقّف نهائياً بطلب المستخدم (2026-07-06). الـcron معطّل والحلقة
  # مقفولة بـ if:false. الصفقات أُغلقت عبر bot_stop.json.
  # لإعادة التشغيل مستقبلاً: احذف سطر if، أعِد الـcron، واحذف bot_stop.json.
  # schedule:
  #   - cron: '*/15 * * * *'
  workflow_dispatch:

concurrency:
  group: okx-bot-loop
  cancel-in-progress: false

permissions:
  contents: write
  actions: write

jobs:
  trade:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # ⛔ البوت متوقّف بطلب المستخدم — انظر التعليق أعلى الملف لإعادة التشغيل.
    if: ${{ false }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: okx_bot/requirements.txt

      - name: Install dependencies
        run: pip install -r okx_bot/requirements.txt

      - name: Run Snowball v22 + Brain
        env:
          OKX_API_KEY:        ${{ secrets.OKX_API_KEY }}
          OKX_API_SECRET:     ${{ secrets.OKX_API_SECRET }}
          OKX_SECRET_KEY:     ${{ secrets.OKX_API_SECRET }}
          OKX_PASSPHRASE:     ${{ secrets.OKX_PASSPHRASE }}
          OKX_IS_DEMO:        ${{ secrets.OKX_IS_DEMO }}
          RISK_MODE:          prime   # الأساس: SL 2%/TP 4%، فلاتر كاملة، فلتر الخاسرين
          BOT_MAX_POSITIONS:  "1"    # عملة واحدة فقط (طلب المستخدم) — رأس المال كاملاً فيها
          BOT_LEVERAGE:       "50"   # رافعة x50 (طلب المستخدم)
          BOT_MIN_COIN_MAX_LEV: "49" # الدخول فقط في عملات تدعم x50+
          BOT_STOP_LOSS_PCT:   "0.005"  # وقف 0.5% (طلب المستخدم)
          BOT_TAKE_PROFIT_PCT: "0.005"  # هدف 0.5% (طلب المستخدم)
          BOT_ALWAYS_IN:      "1"    # تناوب دائم (طلب المستخدم الصريح المتكرر): خروج ← دخول ≤30 ثانية.
                                     # الأولوية دائماً للمرشح مؤكَّد الأطر؛ الاحتياط relaxed ثم weak.
          # 🌐 فتح الكون كله (طلب المستخدم 2026-07-04): جميع العملات الرقمية
          # بسيولة > $50M — بلا قيد الميم وبلا قيد الحركة وبلا سقف سعر.
          BOT_MEME_ONLY:       "0"   # ليس الميم فقط — كل العملات
          BOT_MEME_PREFER:     "0"   # بلا تفضيل ميم
          BOT_MOMENTUM_FILTER: "0"   # بلا قيد الحركة (+10%/−5%)
          BOT_MAX_PRICE_USD:   "0"   # بلا سقف سعر ($0 = معطّل)
          # ⚡ أوامر سوق فورية (طلب المستخدم: يشتري ويبيع الآن). أوامر
          # maker الحدّية كانت لا تُنفَّذ (تنتظر السعر) وتُرفض على العملات
          # الرخيصة بـ«Parameter px error» — فبدا أن البوت لا يتداول.
          ORDER_MODE:          "taker"
          # ⚡ عتبة السرعة 0.3% (كانت 1.0%) — 1% كانت تستبعد ~113 عملة
          # فلا يجد البوت ما يدخله («لا توجد إشارات كافية»). 0.3% يتيح
          # الدخول فوراً مع بقاء ترتيب «الأسرع أولاً» يفضّل السريعة.
          BOT_MIN_ATR_PCT:     "0.2"
          # 🔄 عكس الإشارة (طلب المستخدم): الدخول عكس ما يقوله البوت.
          BOT_REVERSE_SIGNAL:  "1"
          # ⏱️ فاصل إعادة الفحص 15 ثانية (كان 30) — يضمن الدخول التالي
          # بعد إغلاق صفقة خلال ≤30 ثانية حتى مع زمن المعالجة (طلب المستخدم).
          BOT_RESCAN_SECONDS:  "15"
          GH_TOKEN:           ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY:  ${{ github.repository }}
        run: python okx_bot/run_once.py

      - name: Save state to repo
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 - <<'PYEOF'
          import base64, json, os, time, urllib.request, urllib.error

          token = os.environ['GH_TOKEN']
          repo  = os.environ['GITHUB_REPOSITORY']

          for fname in ['bot_status.json', 'brain_memory.json']:
            try:
              with open(fname, 'rb') as f:
                content = base64.b64encode(f.read()).decode()
            except FileNotFoundError:
              continue

            url = f'https://api.github.com/repos/{repo}/contents/{fname}'
            headers = {
              'Authorization': f'token {token}',
              'Accept': 'application/vnd.github.v3+json',
              'Content-Type': 'application/json',
            }

            # Retry up to 5 times on SHA conflict (race condition between concurrent runs)
            for attempt in range(5):
              sha = None
              try:
                req = urllib.request.Request(url, headers=headers)
                with urllib.request.urlopen(req) as r:
                  sha = json.loads(r.read())['sha']
              except Exception:
                pass

              body = {'message': f'bot: update {fname} [skip ci]', 'content': content}
              if sha:
                body['sha'] = sha

              req = urllib.request.Request(url, data=json.dumps(body).encode(),
                                           method='PUT', headers=headers)
              try:
                with urllib.request.urlopen(req) as r:
                  print(f'✅ {fname} updated (attempt {attempt+1})')
                  break
              except urllib.error.HTTPError as e:
                err = e.read().decode()
                if e.code == 409 or 'sha' in err.lower():
                  # SHA conflict — another run saved first, retry with fresh SHA
                  print(f'⚠️ {fname} SHA conflict, retrying... ({attempt+1}/5)')
                  time.sleep(2 ** attempt)
                  continue
                else:
                  print(f'❌ {fname} failed: {err[:200]}')
                  break
          PYEOF

      - name: 🔁 Schedule next run in 30 seconds
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "⏳ انتظار 30 ثانية ثم تشغيل البوت مجدداً..."
          sleep 30
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
            -H "Authorization: token $GH_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Content-Type: application/json" \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/workflows/bot.yml/dispatches" \
            -d '{"ref":"${{ github.ref_name }}"}')
          if [ "$STATUS" = "204" ]; then
            echo "✅ تم جدولة التشغيل القادم بنجاح (HTTP $STATUS)"
          else
            echo "⚠️ فشل الجدولة (HTTP $STATUS) — سيعتمد على الـ cron كبديل"
          fi
